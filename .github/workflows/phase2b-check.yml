name: Phase 2B Runtime Foundation Test
on:
  push:
    branches: ["main"]
    paths:
      - "runtime/**"
      - "runtime-config.js"
      - "index.html"
      - "app.js"
      - "PHASE-2B.md"
      - "scripts/phase2b-smoke.mjs"
      - ".github/workflows/phase2b-check.yml"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check frontend JavaScript syntax
        run: node --check app.js
      - name: Check Worker JavaScript syntax
        run: node --check runtime/worker.js
      - name: Verify generated Core Agent seed is current
        run: |
          cp runtime/seed-core-agents.sql /tmp/seed-before.sql
          node scripts/build-runtime-seed.mjs
          diff -u /tmp/seed-before.sql runtime/seed-core-agents.sql
      - name: Run Phase 2B foundation checks
        run: node scripts/phase2b-smoke.mjs
      - name: Run Phase 2B runtime contract test
        run: node scripts/phase2b-contract.mjs
